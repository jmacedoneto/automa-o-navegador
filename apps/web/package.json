{
  "name": "@autopilot/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "tsc --noEmit --moduleResolution bundler --module ESNext --target ES2020 --jsx react-jsx --allowImportingTsExtensions --isolatedModules --skipLibCheck src/domains/recordings/api.test.ts && vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
